const contextData = {
  toogleSelectedTheme: jest.fn(),
  getSelectedTheme: jest.fn(),
  selectedTheme: 'darkTheme',
  setSelectedTheme: jest.fn(),
  name: 'Sabrina Guia',
  setName: jest.fn(),
  role: 'Front-end Developer',
  setRole: jest.fn(),
};

export default contextData;
